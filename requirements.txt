# ColorGradientTool Requirements
# Core GUI framework
PySide6>=6.5.0

# Color science library for advanced color space conversions and interpolation
coloraide>=3.0.0

# Optional: macOS app icon support (only needed on macOS)
# pyobjc-framework-Cocoa>=9.0; sys_platform == "darwin"